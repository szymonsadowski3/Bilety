<h1> Wszystkie wydarzenia</h1>
<ul>
  <% for event in @events %>
    <li><a href="/events/<%= event.id %>"><%= event.artist %></a> </li>
  <% end %>
</ul>
<%= link_to 'Nowe ogłoszenie', "/events/new" %>